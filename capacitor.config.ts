import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic-angular',
  appName: 'ionic-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
